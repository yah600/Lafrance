import { SvelteComponent, Snippet } from 'svelte';
import { HTMLAttributes } from 'svelte/elements';

export interface Props {
  class?: string;
  onClick?: (e: any) => void;

  /**
   * Component's HTML Element
   *
   * @default 'button'
   */
  component?: string;
  /**
   * Highlights button as active
   *
   * @default false
   */
  active?: boolean;
}


interface SegmentedButtonProps {}
interface SegmentedButtonProps extends Props {}
interface SegmentedButtonEvents extends Record<'',{}>{}

declare class SegmentedButton extends SvelteComponent<
  SegmentedButtonProps & HTMLAttributes<HTMLButtonElement>,
  SegmentedButtonEvents,
  {
    
  }
> {}

export default SegmentedButton;